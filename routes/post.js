// const express = require("express");
// const router = express();
// const { body } = require("express-validator");
// const postControllers = require("../controllers/post");
// const isAuth = require("../middleware/is-auth");
// const upload = require("../middleware/multer");

// router.get("/getAll", isAuth, postControllers.getPosts);

// router.get("/get/:postId", isAuth, postControllers.getPost);

// router.post("/create", isAuth, upload, postControllers.createPost);

// router.put("/edit/:postId", isAuth, upload, postControllers.editPost);

// router.delete("/delete/:postId", isAuth, postControllers.deletePost);

// module.exports = router;
